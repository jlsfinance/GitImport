import React from 'react';

// Automatic permission requesting disabled for Google Play Compliance.
// Permissions must be requested by specific features on demand.
const PermissionRequestor: React.FC = () => {
    return null;
};

export default PermissionRequestor;
